model User {
  id       String @id @default(uuid())
  name     String
  email    String @unique
  password String

  role     Role   @default(staff)
  transaction Transaction[]
}

enum Role {
  admin
  staff
}
